import React from 'react';

export const Footer = () => {
    return (
        <nav className="Navbar">
            <div className="container">
                
            </div>
        </nav>
    );
}

export default Footer;

