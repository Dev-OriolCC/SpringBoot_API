import React from 'react';
import { Link } from 'react-router-dom';

export const Modal = () => {
    return (
        <nav className="Navbar">
            
        </nav>
    );
}

export default Modal;
